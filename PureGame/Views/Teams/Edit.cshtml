@model PureGame.Entity.Team

@{
    ViewData["Title"] = "Edit";
}

<h1>Изменение</h1>

<h4>Team</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id_team" />
            <div class="form-group">
                <label asp-for="team_name" class="control-label"></label>
                <input asp-for="team_name" class="form-control" />
                <span asp-validation-for="team_name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="players_count" class="control-label"></label>
                <input asp-for="players_count" class="form-control" />
                <span asp-validation-for="players_count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="points_count" class="control-label"></label>
                <input asp-for="points_count" class="form-control" />
                <span asp-validation-for="points_count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
